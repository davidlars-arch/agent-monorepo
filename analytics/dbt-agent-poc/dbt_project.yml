name: "dbt_agent_poc"
version: "0.1.0"
config-version: 2

profile: "dbt_agent_poc"

model-paths: ["models"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_agent_poc:
    staging:
      +materialized: view
    marts:
      +materialized: table

seeds:
  dbt_agent_poc:
    +quote_columns: false
