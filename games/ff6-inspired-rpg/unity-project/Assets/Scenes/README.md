# Scenes

Planned Unity scenes:

- `Boot.unity` - loads config and routes to title.
- `Title.unity` - title screen and start command.
- `ObservatoryRoom.unity` - first exploration room with Mira NPC.
- `BattleTest.unity` - scripted battle against Clockwork Imp.

These are not generated yet because Unity Editor is not installed on this machine. Create them from Unity and keep the names above so the scripts and web shell stay aligned.
